import { purple, teal } from "@radix-ui/colors";
import { styled } from "@stitches/react";

export const StyledCard = styled('div', {
    borderRadius: '0.5em',
    marginLeft: 'auto',
    marginRight: 'auto',
    paddingTop: '0.1em',
    paddingRight: '5em',
    paddingBottom: '1.5em',
    paddingLeft: '5em',
    boxShadow: '0 0 1.2em 0 rgba(0,0,0,0.5)',
    backgroundColor: teal.teal4,
});